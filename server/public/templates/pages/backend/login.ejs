<!DOCTYPE html>
<html lang="es">
	<head>
		<% include ../../partials/backend/head_backend %>
		<link rel="stylesheet" href="/static/css/src/login_b.css" />
	</head>

	<body>
		<!-- Login -->
		<div class="row container">
			<div class="section white container z-depth-2">
				<h2 class="center-align red-text text-accent-1">Museums for Everyone</h2>
				<h3 class="center-align red-text text-accent-1">Administración</h3>
				<p class="grey-text text-lighten-1">El sistema distingue automáticamente los privilegios de la cuenta con la que proceda a hacer el ingreso.</p>

				<form method="post" action="/user/login/authentication/">
					<!-- Nombre de Usuario -->
					<div class="row">
						<div class="input-field col s12">
							<i class="material-icons prefix">account_circle</i>
							<input id="email_user" name="email_user" type="text" class="validate">
							<label for="email_user">Email</label>
						</div>
					</div>

					<!-- Contraseña -->
					<div class="row">
						<div class="input-field col s12">
							<i class="material-icons prefix">vpn_key</i>
							<input id="password_user" name="password_user" type="password" class="validate">
							<label for="password_user">Contraseña</label>
						</div>
					</div>

					<% if(typeof error !== 'undefined') { %>
						<p id="errorText" class="red-text text-lighten-1"><%= error %></p>
					<% } %>

					<!-- Submit Button -->
					<div class="row center-align">
						<button id="submit" class="btn waves-effect waves-light" type="submit" name="submit_user">Iniciar Sesión
							<i class="material-icons right">send</i>
						</button>
					</div>
				</form>
			</div>
		</div>

		<% include ../../partials/backend/script_backend %>
	</body>
</html>
